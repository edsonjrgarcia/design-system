import type { StoryObj } from '@storybook/react'
import { Button } from '@media-recovery-ui/react'

export default {
  title: 'Form/Button',
  component: Button,
  args: {
    children: 'Send',
  }
}

export const Primary: StoryObj = {}

export const Secondary: StoryObj = {}